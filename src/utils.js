const DIFF_TYPE_DICT = {
  sameKeySameValue: 'same key, same value',
  sameKeyDiffValue: 'same key, different value',
  onlyKey1: 'value only in first file',
  onlyKey2: 'value only in second file',
};

export default DIFF_TYPE_DICT;
